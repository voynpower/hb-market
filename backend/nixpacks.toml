[phases.setup]
nixPkgs = ["nodejs_20", "openssl"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npx prisma generate", "npm run build"]

[start]
# Ensure database schema is synced before starting the server
cmd = "npx prisma generate && npx prisma db push --accept-data-loss && node dist/main"
